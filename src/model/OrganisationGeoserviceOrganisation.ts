import { Organisation } from '../model/Organisation';

/**
* L\'organisation liée à ce geoservice.
*/
export type OrganisationGeoserviceOrganisation = Organisation & { 
};

export enum TypeEnum {
    Sis = 'sis'
}
