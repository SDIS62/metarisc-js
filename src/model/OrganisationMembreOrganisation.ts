import { Organisation } from '../model/Organisation';

/**
* Organisation liée au membre.
*/
export type OrganisationMembreOrganisation = Organisation & { 
};

export enum TypeEnum {
    Sis = 'sis'
}
